<h1><%= @movie.title %></h1>
<p><%= @movie.description %></p>

<div class="actions">
  <%= link_to 'Edit Movie', edit_movie_path(@movie) %>
  <%= link_to 'Delete Movie', @movie ,class: 'delete', method: :delete, data: { confirm: 'Are you sure you want to delete this movie!'} %>
</div>
